version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: database
    restart: always
    env_file:
      - .env

    ports:
      - "3303:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - app-network

  redis:
    image: redis:7.2
    container_name: redis-sep490
    restart: always
    ports:
      - "6379:6379"
    env_file:
      - .env
    networks:
      - app-network

  app:
    build: .
    container_name: be
    restart: always
    depends_on:
      - mysql
    ports:
      - "8080:8080"
#    environment:
#      SPRING-PROFILES-ACTIVE: local

    networks:
      - app-network

volumes:
  mysql_data:

networks:
  app-network:



#local: SPRING_PROFILE=local ./gradlew bootRun

#dev:
#-export SPRING_PROFILE=dev
#-docker-compose up --build

#prod:
#export SPRING_PROFILE=prod
#docker-compose up --build -d